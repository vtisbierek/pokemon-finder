export interface PokemonData{
    name: string;
    image: string;
    types: string[];
    stats: number[];
}

export interface PageStyle{
    background: string;
    border: string;
    statNames: string[]; 
}